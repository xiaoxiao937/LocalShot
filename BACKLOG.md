# LocalShot 待统一处理的问题

记录日期：2026-08-20

> 以下需求已统一实现于 0.1.11，保留本文件作为验收记录。

## 1. 屏幕文字拾取交互

- 当前问题：文字识别完成后会直接把全部结果复制到剪贴板，缺少确认与选择步骤。
- 期望效果：参考微信截图，识别结束后打开独立的文字结果窗口。
- 用户应能在窗口中查看识别结果、选择想要的部分，再主动复制。
- 不应在用户确认前自动覆盖剪贴板。

## 2. 截图底部工具栏精简与排序

- 删除“线条”工具。
- 删除“圆圈/椭圆”工具。
- 将“文字”工具调整为第二个。
- 其余工具的最终顺序在统一修改时结合现有数字快捷键一并确认。

## 3. 候选区域的视觉识别

- 当前问题：候选区域识别过度依赖 macOS 认为目标是一个真实窗口。
- 期望效果：参考微信截图，更多依据屏幕内容的形状、边界和矩形轮廓生成候选区域。
- 即使目标不是系统级窗口，只要视觉上是明确的面板或矩形内容区域，也应有机会被自动匹配。
- 统一修改时评估视觉轮廓识别与系统窗口信息的组合策略；不能只保留系统窗口识别。

## 4. 颜色选择控件尺寸

- 当前问题：底部工具栏中的颜色候选框尺寸过大，与相邻图标不统一，视觉上显得突兀。
- 期望效果：缩小颜色控件，使其高度、占位范围和视觉重量与旁边的工具图标协调一致。
- 保留颜色预览和颜色选择能力，不因缩小控件影响点击与操作。

## 5. 候选区域提示框被遮挡

- 当前问题：截图候选区域的提示框有一部分被上方内容遮盖，提示信息不能完整显示。
- 期望效果：提示框始终完整可见，不被菜单栏、屏幕边缘、候选区域或其他浮层裁切和覆盖。
- 统一修改时检查提示框的窗口层级、屏幕可见区域约束和靠近顶部时的自动换位逻辑。

## 6. 回车键完成截图

- 当前问题：截图完成并进入编辑状态后，按 Return/Enter 无法触发“对勾”操作。
- 期望效果：未处于文字输入状态时，Return 和数字键盘 Enter 均等效于点击“对勾”，即复制最终 PNG 并结束本次截图。
- 文字输入框正在编辑时，回车应优先完成或提交当前文字，不能同时结束整个截图会话。

## 7. 隐藏菜单栏图标后的恢复入口

- 当前问题：菜单栏图标被手动隐藏后，应用没有其他控制界面入口。
- 处理结果：0.1.12 增加固定全局快捷键 `⌘⇧0` 打开设置。
- 再次从“应用程序”或 Spotlight 打开 LocalShot 时，也会显示设置窗口。
- 应用继续采用 accessory 模式，不在 Dock 中长期显示图标。
