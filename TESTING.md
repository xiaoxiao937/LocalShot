# LocalShot 手工验收清单

## 本次已验证（2026-08-15）

- Xcode 27.0 beta 3 成功打开 `LocalShot.xcodeproj`，Scheme 为 `LocalShot`，目标为 `My Mac`。
- Debug App 成功编译并保持运行。
- 首次隐私欢迎页内容与按钮显示正常。
- 点击“开始使用”前未出现录屏、辅助功能、输入监控、麦克风或摄像头授权请求。
- 运行期间未发现 TCP 或 UDP 网络连接。

截图和编辑流程尚未自动触发，因为首次截图会申请系统录屏权限；请由用户确认后按下列项目验收。

## 在 Xcode Beta 中运行

1. 使用 Xcode Beta 打开项目根目录的 `LocalShot.xcodeproj`。
2. Scheme 选择 `LocalShot`，运行目标选择 `My Mac`。
3. 点击 Run。应用不会显示 Dock 图标，菜单栏会出现取景框图标。
4. 首次启动会显示隐私欢迎页，但不会立即申请任何权限。
5. 首次点击截图时，按系统提示授予“屏幕与系统音频录制”权限。

## 基础检查

- 菜单栏包含区域、全屏、最近截图、设置、隐私说明和退出，不再包含独立窗口截图。
- 默认快捷键为区域 `⌘⇧1`、全屏 `⌘⇧3`。
- 设置中可以把两个动作分别改为 `⌘⇧1` 至 `⌘⇧9`，重复键会被拒绝。
- 未主动截图前，不应出现屏幕权限请求。
- 系统设置中不应出现 LocalShot 的辅助功能、输入监控、麦克风或摄像头授权请求。

## 截图检查

- 区域截图：选区内始终显示清晰的实时桌面，选区外仅半透明变暗，不应出现整屏柔化。
- 移动鼠标时自动高亮指针下最前面的普通窗口；单击截取该窗口在当前屏幕内的可见边界。
- 应用从后台唤起后第一次单击候选窗口也应立即完成截图，不需要再次点击。
- 截图遮罩显示期间，单击、拖动和键盘输入均不得操作遮罩下方的实际应用。
- 进入截图后立即或在移动鼠标、单击候选窗口前后按 Esc，均应退出截图遮罩。
- 从任意位置拖动超过 4 点即切换为自由框选；松开后在原选区位置进入标注状态，Esc 可取消。
- 全屏截图：截取鼠标所在显示器。
- Retina 屏幕导出图片保持原始像素尺寸。
- 外接显示器上能正常截取当前显示器。

## 编辑检查

- 标注画布位于原选区，悬浮工具条优先贴在选区下方并以选区水平中心对齐；空间不足时移至上方或限制在屏幕内。
- Retina 截图预览文字和细线清晰，不出现先截整屏、再缩放背景导致的模糊。
- Retina 选区导出像素宽高应约为选区点尺寸的 2 倍；普通屏幕为 1 倍。
- 截图和带标注导出均保持正向，不得上下颠倒。
- 截图完成后默认显示手形移动工具，拖动截图时悬浮工具条同步移动，不会自动生成箭头。
- Finder、系统设置和欢迎页能显示 LocalShot 应用图标。
- 未添加标注时，保存 PNG 的像素尺寸与捕获结果完全一致。
- 添加标注后，保存 PNG 仍保持原始像素尺寸，标注位置与预览一致。
- 直线、矩形、椭圆和画笔可正常绘制；箭头应为圆润的一体式实心造型。
- 文字工具在画布内直接出现输入框；中文输入、回车提交、点击别处提交与 Esc 取消均正常。
- 文字输入框应为紧凑的系统材质胶囊，不出现横跨画布的深色粗边矩形。
- 文字在编辑预览、对勾复制和下载 PNG 中均保持正向，不得上下倒置。
- 使用移动工具点击已有文字会显示虚线选中框；拖动可调整文字位置，且不会移动整张截图。
- 拖动文字选中框右下角控制点可连续调整字号，文字不能缩小到不可见或放大到失控。
- 回车或点击别处完成文字后，工具栏自动选中第一个移动工具，新文字保持选中。
- 未输入文字时，数字键 `1–9` 应依次切换九个编辑工具；输入框中数字仍可正常录入。
- 马赛克覆盖区域不可辨认。
- 裁剪后画布尺寸正确。
- “提取文字”可通过本机 Vision 识别中英文并复制到剪贴板，不产生网络连接。
- “贴图”隐藏工具条并留下可拖动的置顶图像，Esc 可关闭。
- 贴图右上角关闭按钮、Esc 和 `⌘W` 均可关闭贴图。
- 截图编辑窗口可通过 `⌘W` 关闭；文字输入期间 Esc 只取消文字输入。
- 撤销与重做顺序正确。
- 绿色对勾会复制 PNG 并关闭编辑器；界面不再出现独立复制按钮。
- 点击绿色对勾复制并完成时不播放系统提示音。
- 下载按钮只保存 PNG，不应修改剪贴板；下载后编辑器继续保留。
- 工具条各组垂直居中且间距一致；macOS 26 及以上显示原生 Liquid Glass，旧系统显示系统材质回退。
- 顶部截图提示和尺寸标签使用紧凑圆角胶囊，不应再出现整行文字硬铺矩形底色。
- 对勾复制出的 PNG 与下载保存的 PNG 内容一致且方向正确。
- 连续使用对勾完成截图不应在应用容器内新增截图、缩略图或历史缓存文件。

## 隐私检查

在终端运行：

```sh
Scripts/audit_privacy.sh
```

预期结果为“隐私审计通过”。运行应用后，也可以使用系统防火墙或网络监控工具确认 LocalShot 没有网络连接。
